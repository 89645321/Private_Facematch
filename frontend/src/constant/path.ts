const logIn = "/login";
const photoUpload = "/upload";
const idcardUpload = "/idcardupload";
const account = "/account";
const signUp = "/signup";

const paths = {
  logIn,
  signUp,
  photoUpload,
  idcardUpload,
  account
};

export default paths;