const logIn = "/login";
const photoUpload = "/photoupload";
const idcardUpload = "/idcardupload";
const account = "/account";

const paths = {
  logIn,
  photoUpload,
  idcardUpload,
  account
};

export default paths;